// ゲーム全体で共有するドメイン型の置き場。
// 例（必要になったら定義する）:
// export interface Character { id: string; name: string; level: number; hp: number; atk: number }
// export interface Stage { id: string; name: string; nextStageIds: string[] }
// export interface Formation { id: string; slots: (string | null)[] } // characterId[]
export {}
